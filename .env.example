# Optional if the endpoint requires authentication.
HILMA_GRAPHQL_URL=https://hankintailmoitukset.fi/api/graphql

# Add only if your Hilma/API setup requires it.
# HILMA_API_KEY=

# LLM analysis is intentionally not wired into the MVP scaffold yet.
# OPENAI_API_KEY=
